{
  "$schema": "https://developers.cloudflare.com/workers/wrangler/config-schema.json",
  "name": "mvneves-dev",
  "compatibility_date": "2026-08-09",
  // Runs only for "/" and "/lang" (assets.run_worker_first): an arrival at / whose browser's top language is
  // Portuguese goes to /pt-br/ unless the visitor chose a language; POST /lang stores that choice. Every other path is
  // served straight from the assets without invoking the Worker.
  "main": "worker/index.js",
  // Production is served only on the custom domains below: no workers.dev URL, no per-version preview URLs.
  "workers_dev": false,
  "preview_urls": false,
  "routes": [
    { "pattern": "mvneves.dev", "custom_domain": true },
    // www serves the same assets; every page's canonical URL points at the apex.
    // A dashboard Redirect Rule (www -> apex 301) can replace this later.
    { "pattern": "www.mvneves.dev", "custom_domain": true }
  ],
  "assets": {
    "directory": "./dist",
    "binding": "ASSETS",
    "run_worker_first": ["/", "/lang"],
    "not_found_handling": "404-page"
  },
  "env": {
    "staging": {
      "name": "mvneves-dev-staging",
      // Named envs inherit top-level "routes" — override so staging never claims the prod custom domains.
      "routes": [],
      "workers_dev": true
    }
  }
}
