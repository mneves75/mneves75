# 2026-08-10 — 1.4.0 redesign shipped to production

- Redesigned all 6 page families as "Terminal on Paper" (herdr.dev base + CT signature devices, graphite/bone/copper). Foundation + home by lead session; work/detail and about/recs/contact/404 by two parallel lanes with exclusive file ownership and scoped styles.
- Real bugs fixed beyond styling: pt-BR accents in fallback fonts (latin subsets vendored), no-JS reveal blanking (reveal-pending), 3 AA contrast failures, nav hover/active ambiguity (user-reported), theme-color not following manual toggle.
- pt-BR copy review: "Confiado para liderar"→"Confiam em mim para liderar", "imposto"→"pedágio de idioma", "banking"→"bancos", "vale trabalhar"→"vale a pena trabalhar", "email"→"e-mail", work-index intro, diagram keeps "teach-back".
- New assets: MN chip favicon, EN + pt-BR OG cards (rendered via headless Chrome from HTML using vendored fonts).
- Verification: check/build/test green ×3 rounds; agent-browser pass 6 families × light/dark × 1280/390 × EN/pt; interactions (filters, decoder, MN://TERMINAL, palette, reduced-motion); 16 contrast pairs computed; autoreview clean twice (0.99).
- Deploys: staging `6f9b14bd` → `08b1b3e6`; prod `08136256` → `01fd713e` + **custom domain attached** (apex had no DNS record before — discovered during prod smoke, fixed via wrangler routes).
- Tree left uncommitted at user's flow; `ship` pending.

## v1.4.1 — principles pt-BR + wrangler routes trap
- `principles` in `site.ts` had EN duplicated into `pt`; translated 4 strings (user-chosen: "Entregar o bom é melhor que não entregar o perfeito."). Bumped to 1.4.1, changelog updated.
- **TRAP**: `wrangler deploy --env staging` inherited top-level `routes` and CLAIMED the prod custom domains (apex+www pointed at staging worker). Fixed: `env.staging` now sets `routes: []` + `workers_dev: true`; prod redeployed to reclaim domains, staging redeployed to workers.dev.
- Verified: prod pt-br md5 == local dist (initial miss was stale cf-cache HIT), www + staging serve all 4 translations, EN untouched. Prod `773e7060`, staging `f248abfc`. Tree still uncommitted.
