# 2026-08-13 — home masthead clip

- Desktop clipped `MARCUS NEVES` at the last `S`. Mobile was fine.
- Cause: `.mn-masthead` used `vw` (and a scroll-driven `font-stretch` 80%→125%) while living in the 1160px `.page-shell` column. At 1440px the name was wider than the column.
- Fix: `container-type: inline-size` on `.masthead-band`, `font-size: clamp(2rem, 10.6cqi, 8.5rem)`, drop the stretch animation.
- Live measure after deploy: 1025px text in 1064px available at 1440px.
- Staging `d0edd9c8…` → prod `6673aeac…`.
