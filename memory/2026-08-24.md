# 2026-08-24 — 1.3.0: hay + devtrim, hashed CSP

- Local `main` was the pre-rewrite history (remote rewritten 2026-08-19); realigned with `git checkout -B main origin/main` after a backup branch. Content diff was only README + docs/RESEARCH.md.
- Added `hay` (featured) and `devtrim` case studies written from their READMEs; pt-BR diagram labels added (build throws otherwise).
- CSP: `scripts/csp-headers.mjs` post-build hashes the 3 inline scripts (theme boot, layout module, work filter) into `dist/_headers`. Proven with `wrangler@latest dev` + agent-browser: corrupting each hash disabled exactly that script (theme boot → no data-theme; module → toggle dead; filter → 0 rows hidden), restored → all work.
- `.sr-only` class replaces the terminal's inline style attribute; terminal echo escapes `&`.
- Full-page screenshots show a blank gap where the diagram sits — it is `.reveal-pending` until intersected, not a layout bug.
